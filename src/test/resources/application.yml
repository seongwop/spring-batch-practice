spring:
  batch:
    jdbc:
      initialize-schema: always
    job:
      name: executePointReservationJob
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create
#  datasource:
#    url: jdbc:h2:mem:testdb;MODE=MYSQL;
#    driverClassName: org.h2.Driver
#  h2:
#    console:
#      enabled: true
  datasource:
    url: jdbc:mysql://127.0.0.1:3307/point?useUnicode=true&characterEncoding=utf8
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: password